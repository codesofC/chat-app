import MyAccordion from "../MyAccordion";

const ChatSettings = () => {
  return (
    <MyAccordion title="Chat Settings">
      <p>Chat Settings</p>
    </MyAccordion>
  );
};

export default ChatSettings;
